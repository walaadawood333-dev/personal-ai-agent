````markdown
# دليل المساهمة

شكراً لاهتمامك بالمساهمة في مشروع وكيل الذكاء الاصطناعي الشخصي! 🎉

## كيفية المساهمة

### 1. الإبلاغ عن الأخطاء

إذا وجدت خطأ:

1. تحقق من أنه لم يتم الإبلاغ عنه بالفعل في Issues
2. افتح Issue جديد مع توفير:
   - وصف واضح للمشكلة
   - خطوات إعادة الإنتاج
   - السلوك المتوقع والفعلي
   - معلومات النظام (Python version, etc)

### 2. اقتراح ميزات جديدة

للاقتراح بميزة:

1. افتح Issue بعنوان "Feature: ..."
2. اشرح الميزة المقترحة والفائدة منها
3. قدم أمثلة إن أمكن

### 3. المساهمة في الكود

#### خطوات الإجراء:

```bash
# 1. اعمل Fork للمستودع
git clone https://github.com/your-username/personal-ai-agent.git
cd personal-ai-agent

# 2. أنشئ فرع جديد
git checkout -b feature/your-feature-name

# 3. ثبت المتطلبات
pip install -r requirements.txt

# 4. اكتب الكود
# تأكد من اتباع معايير الكود

# 5. اختبر التغييرات
pytest tests/

# 6. اجعل الكود منسقاً
black core/ main.py tests/

# 7. اختبر الجودة
flake8 core/ main.py

# 8. اعمل Commit
git add .
git commit -m "Add feature: description"

# 9. اعمل Push
git push origin feature/your-feature-name

# 10. افتح Pull Request
```

## معايير الكود

### النمط والتنسيق

- استخدم [Black](https://black.readthedocs.io/) للتنسيق
- اتبع [PEP 8](https://pep8.org/) لإرشادات الأسلوب
- استخدم [Flake8](https://flake8.pycqa.org/) للتحقق من الجودة

### التعليقات والتوثيق

```python
def my_function(param: str) -> str:
    """
    وصف مختصر للدالة
    
    Args:
        param: وصف المعامل
    
    Returns:
        وصف القيمة المرجعة
    
    Raises:
        ValueError: متى يتم رفع الخطأ
    """
    # تعليق واضح للكود المعقد
    return param.upper()
```

### الاختبارات

- كل ميزة جديدة يجب أن تأتي مع اختبارات
- الاختبارات يجب أن تغطي الحالات الطبيعية والحدية
- استخدم pytest للاختبارات

```python
def test_my_feature():
    """اختبار الميزة الجديدة"""
    assert my_function("input") == "expected_output"
```

## رسائل الـ Commits

اتبع هذا الصيغة:

```
[Type] Description

Optional longer description explaining the changes.

Fixes #123
```

### أنواع الـ Commits:

- `fix:` إصلاح الأخطاء
- `feat:` ميزة جديدة
- `docs:` تحديثات التوثيق
- `style:` تنسيق الكود
- `refactor:` إعادة هيكلة الكود
- `test:` إضافة أو تحديث الاختبارات
- `chore:` تحديثات البناء والتبعيات

**أمثلة:**

```
feat: إضافة أداة البحث على الويب
fix: إصلاح خطأ في معالجة الذاكرة
docs: تحديث التعليقات في الوكيل
```

## Pull Request

### قبل فتح PR:

- [ ] تأكد من أن الفرع يحتوي على آخر التحديثات من main
- [ ] اختبر جميع التغييرات محلياً
- [ ] تأكد من نجاح جميع الاختبارات
- [ ] تحقق من الكود مع Black و Flake8

### قالب PR:

```markdown
## الوصف
وصف قصير للتغييرات

## نوع التغيير
- [ ] إصلاح خطأ
- [ ] ميزة جديدة
- [ ] تكسير التوافق
- [ ] تحديث التوثيق

## الاختبار
كيفية اختبار التغييرات:

## Checklist
- [ ] تم اختبار الكود محلياً
- [ ] تم تحديث التوثيق
- [ ] تم إضافة/تحديث الاختبارات
- [ ] الكود يتبع معايير المشروع
- [ ] لا توجد رسائل خطأ/تحذير

## المشاكل المرتبطة
Fixes #123
```

## أسلوب الاتصال

- كن محترماً وودوداً
- ركز على الفكرة وليس الشخص
- تقبل الانتقادات البناءة
- ساعد الآخرين على التعلم

## القائمة البريدية

للحصول على التحديثات الأخيرة:

- Star المستودع ⭐
- شارك المشروع مع الآخرين
- اتبع أحدث الأخبار

## أسئلة؟

لا تتردد في:
- فتح Issue للسؤال
- اترك تعليق على PR
- تواصل عبر المشروع

---

**شكراً على مساهمتك! 🙏**
````
